<div class="container my-5">
  <div class="card">
    <div class="card-header">Create New Patient<%= link_to('Back', patients_path, class: "btn btn-primary float-right btn-sm") %></div>
    <div class="card-body">
      <%= simple_form_for invitation do |f| %>
        <%= f.input :email, placeholder: 'user@domain.com' %>
      <% end %>
    </div>
  </div>
</div>
