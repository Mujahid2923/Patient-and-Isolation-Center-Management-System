<div class="container my-5">
  <div class="card">
    <div class="card-header">Update Patient<%= link_to('Back', patients_path, class: "btn btn-primary float-right btn-sm") %></div>
    <div class="card-body">
      <%= render('form', patient: @patient) %>
    </div>
  </div>
</div>
