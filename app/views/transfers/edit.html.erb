<div class="container my-5">
  <div class="card">
    <div class="card-header">Update Transfer<%= link_to('Back', :back, class: "btn btn-primary float-right btn-sm") %></div>
    <div class="card-body">
      <%= render('form', transfer: @transfer) %>
    </div>
  </div>
</div>
